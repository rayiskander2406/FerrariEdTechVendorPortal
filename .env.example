# =============================================================================
# SchoolDay Vendor Portal - Environment Variables
# =============================================================================
#
# Copy this file to .env and update with your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env files with real credentials to git!
#
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Main Database (PostgreSQL)
# Used by: Prisma ORM for application data
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://schoolday:schoolday_password@localhost:5434/schoolday_dev

# Vault Database (PostgreSQL - Separate for Security)
# Used by: Token mapping and security operations
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
VAULT_DATABASE_URL=postgresql://vault:vault_password@localhost:5433/schoolday_vault

# Docker Compose Passwords (optional - defaults provided in docker-compose.yml)
# Uncomment and set for production or enhanced security
# POSTGRES_PASSWORD=your_secure_main_password
# VAULT_POSTGRES_PASSWORD=your_secure_vault_password

# =============================================================================
# API KEYS
# =============================================================================

# Anthropic Claude API Key
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# =============================================================================
# ENVIRONMENT
# =============================================================================

# Application Environment
# Options: development | test | production
NODE_ENV=development

# =============================================================================
# FEATURE FLAGS (Optional)
# =============================================================================

# Use mock database (in-memory) instead of PostgreSQL
# Set to 'true' for quick demos without database setup
# USE_MOCK_DB=false

# Enable shadow mode for schema validation
# SHADOW_MODE=false
