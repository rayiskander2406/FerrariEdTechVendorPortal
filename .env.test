# =============================================================================
# SchoolDay Vendor Portal - Test Environment
# =============================================================================
#
# Used by: Vitest test runner
# Database: SQLite (fast, isolated test runs)
#
# =============================================================================

# Test Database (SQLite for fast isolated tests)
# Each test file gets a clean database
DATABASE_URL="file:./prisma/test.db"

# Vault Database (SQLite for tests - mirrors main test pattern)
VAULT_DATABASE_URL="file:./prisma/vault-test.db"

# Test Environment
NODE_ENV=test

# Enable shadow mode verification tests
SHADOW_MODE=true
